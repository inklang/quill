[package]
name = "ink.paper"
version = "0.1.0"

[grammar]
entry = "src/grammar.ts"
output = "dist/paper/grammar.ir.json"

[targets.paper]
entry = "org.inklang.paper.PaperBridge"
jar = "runtime/paper/build/libs/ink-paper-0.1.0.jar"
